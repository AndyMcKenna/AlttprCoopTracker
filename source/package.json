{
  "name": "alttp-coop-tracker",
  "version": "1.0.0",
  "private": true,
  "description": "Game tables, tooling and browser tests for the ALTTP co-op tracker; the app itself is the .NET solution alongside.",
  "scripts": {
    "test": "node test/smoke.test.js",
    "export-gamedata": "node scripts/export-gamedata.js",
    "build-sprites": "node scripts/build-spritesheet.js",
    "test:e2e": "playwright test"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "@playwright/test": "1.63.0",
    "jpeg-js": "^0.4.4",
    "pngjs": "^7.0.0"
  }
}
